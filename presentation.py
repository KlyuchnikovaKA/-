import streamlit as st

def presentation():
    st.title("Система для предиктивного мониторинга состояния оборудования")

    # Введение
    st.header("1. Обзор проекта")
    st.write(
        "Данный проект предназначен для прогнозирования возможных отказов "
        "промышленного оборудования с использованием данных из датасета AI4I 2020 Predictive Maintenance. "
        "Дата обновления: 14 мая 2025 года."
    )
    st.image(
        "https://via.placeholder.com/300x200.png?text=AI4I+Dataset",
        caption="Датасет AI4I 2020 Predictive Maintenance"
    )

    # Этапы реализации
    st.header("2. Основные этапы работы")
    st.markdown(
        """
        - **Загрузка данных**: пользователю предоставляется возможность загрузить CSV-файл через удобный веб-интерфейс.
        - **Подготовка данных**: очистка, удаление лишних признаков, кодировка категориальных переменных, масштабирование числовых данных.
        - **Обучение моделей**: использование четырёх алгоритмов машинного обучения — Logistic Regression, Random Forest, XGBoost и Support Vector Machine (SVM).
        - **Оценка качества**: сравнение эффективности моделей по метрикам точности (Accuracy), матрице ошибок, отчету классификации (precision, recall, F1-score) и ROC-AUC.
        - **Визуализация результатов**: отображение матриц ошибок, ROC-кривых и распределения входных признаков.
        - **Прогнозирование**: возможность делать предсказания на новых данных с указанием вероятности отказа оборудования.
        """
    )

    # Ключевые преимущества
    st.header("3. Ключевые преимущества системы")
    st.write(
        "Реализованная система демонстрирует высокие показатели точности — до 97%, "
        "а также предоставляет интуитивно понятный интерфейс для анализа и прогнозирования."
    )
    st.slider("Текущая точность модели", 0.0, 1.0, 0.97, disabled=True)

    # Итоги и дальнейшие планы
    st.header("4. Итоги и планы на будущее")
    st.write(
        "Проект успешно реализован с использованием четырёх моделей и комплексного набора метрик оценки. "
        "В дальнейшем планируется расширить используемые датасеты, улучшить модели и интегрировать систему в производственные процессы."
    )

presentation()
